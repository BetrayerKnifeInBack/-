# Исходные данные (глобальные переменные, как указано в задании)
documents = [
    {'type': 'passport', 'number': '2207 876234', 'name': 'Василий Гупкин'},
    {'type': 'invoice', 'number': '11-2', 'name': 'Геннадий Покемонов'},
    {'type': 'insurance', 'number': '10006', 'name': 'Аристарх Павлов'}
]

directories = {
    '1': ['2207 876234', '11-2'],
    '2': ['10006'],
    '3': []
}

# Функция для поиска владельца документа по номеру
def get_owner_by_document_number(doc_number):
    """
    Ищет владельца документа по его номеру.
    
    Args:
        doc_number (str): Номер документа
        
    Returns:
        str: Имя владельца или сообщение об ошибке
    """
    for document in documents:
        if document['number'] == doc_number:
            return document['name']
    return "владелец не найден"

# Функция для поиска полки по номеру документа
def get_shelf_by_document_number(doc_number):
    """
    Ищет полку, на которой хранится документ.
    
    Args:
        doc_number (str): Номер документа
        
    Returns:
        str: Номер полки или сообщение об ошибке
    """
    for shelf, docs_on_shelf in directories.items():
        if doc_number in docs_on_shelf:
            return shelf
    return "документ не найден на полках"

# Основная функция для обработки пользовательских команд
def main():
    """
    Основной цикл программы для обработки пользовательского ввода.
    """
    print("Добро пожаловать в систему учета документов!")
    print("Доступные команды:")
    print("  p - найти владельца документа")
    print("  s - найти полку с документом")
    print("  q - выход из программы")
    print("-" * 50)
    
    while True:
        # Получаем команду от пользователя
        command = input("\nВведите команду: ").strip().lower()
        
        # Выход из программы
        if command == 'q':
            print("Выход из программы. До свидания!")
            break
        
        # Поиск владельца документа
        elif command == 'p':
            doc_number = input("Введите номер документа: ").strip()
            owner = get_owner_by_document_number(doc_number)
            print(f"Владелец документа: {owner}")
        
        # Поиск полки с документом
        elif command == 's':
            doc_number = input("Введите номер документа: ").strip()
            shelf = get_shelf_by_document_number(doc_number)
            print(f"Документ хранится на полке: {shelf}")
        
        # Неизвестная команда
        else:
            print("Неизвестная команда. Попробуйте снова.")
            print("Доступные команды: p, s, q")

# Запуск программы
if name == "main":
    main()
